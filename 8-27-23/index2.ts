// Given an array of numbers [1, 2, 3, 4, 5], use the map method to 
// create a new array where each number is multiplied by 2
var num:number[]=[1,2,3,4,5]
let newArr= num.map((x)=>{
        return x*2})
        console.log(newArr) //new arrat with each element multiplied by 2